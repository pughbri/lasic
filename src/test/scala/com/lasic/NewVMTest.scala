package com.lasic

import org.scalatest.FunSuite
import org.scalatest.junit.JUnitRunner
import org.junit.runner.RunWith
//import org.junit.runner.RunWith

@RunWith(classOf[JUnitRunner])  class NewVMTest extends FunSuite {
  test("A random test") {
    assert(true)
  }
}